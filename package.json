{
    "scripts": {
        "build": "npm run build-bundle",
        "watch": "watch-run -i -p 'assets/app/css/**/*' npm run build && watch-run -i -p 'assets/app/js/**/*' npm run build",
        "build-bundle": "node modules/App/assets/build.bundle.js"
    },
    "dependencies": {
        "postcss": "^8.2.13",
        "postcss-import": "^14.0.1",
        "postcss-url": "^10.1.3",
        "rollup": "^2.45.2",
        "watch-run": "^1.2.5"
    }
}
